# User plugins

Place user-created Python plugin files in this directory.  They are loaded in
addition to the built-in application plugins.  See
`docs/PLUGIN_DEVELOPMENT_GUIDE.md` for the plugin interface and examples.

Release updates preserve this directory and do not remove user plugins.
